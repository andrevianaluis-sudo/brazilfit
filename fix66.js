const fs = require("fs"); const file = "C:/Users/viana/BRAZILFIT/backend/src/routes/checkins.js"; let c = fs.readFileSync(file, "utf8"); c = c.replace("[existing.id](http://existing.id)", "existing.id"); c = c.replace("[existing.id](http://existing.id)", "existing.id"); c = c.replace("[existing.id](http://existing.id)", "existing.id"); fs.writeFileSync(file, c, "utf8"); console.log("Done, still has hyperlink:", c.includes("[existing.id](http://existing.id)"));