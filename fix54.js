const fs=require("fs");const file="C:/Users/viana/BRAZILFIT/frontend/src/pages/client/ClientLayout.jsx";let c=fs.readFileSync(file,"utf8");c=c.replace("  const handleLogout = () => { logout(); navigate(\"/login\"); };","  useEffect(()=>{ api.get(\"/pt/client-notifications\").then(r=>{setUnreadCount(r.data.unreadCount||0);setNotifications(r.data.notifications||[]);}).catch(()=>{}); },[]);\n\n  const handleLogout = () => { logout(); navigate(\"/login\"); };");fs.writeFileSync(file,c,"utf8");console.log("Done:",c.includes("client-notifications"));