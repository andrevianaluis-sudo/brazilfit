const fs = require("fs"); const file = "C:/Users/viana/BRAZILFIT/frontend/src/pages/client/ClientLayout.jsx"; let c = fs.readFileSync(file, "utf8"); c = c.replace("const [showProfileModal, setShowProfileModal] = useState(false);", "const [showProfileModal, setShowProfileModal] = useState(false);\n  const [unreadCount, setUnreadCount] = useState(0);\n  const [notifications, setNotifications] = useState([]);\n  const [showNotifs, setShowNotifs] = useState(false);"); c = c.replace("  useEffect(() => {", "  useEffect(() => {\n    api.get(\"/pt/client-notifications\").then(r => { setUnreadCount(r.data.unreadCount||0); setNotifications(r.data.notifications||[]); }).catch(()=>{});\n  }, []);\n\n  useEffect(() => {"); fs.writeFileSync(file, c, "utf8"); console.log("Done:", c.includes("unreadCount"));