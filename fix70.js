const fs=require("fs");const f="C:/Users/viana/BRAZILFIT/backend/src/routes/checkins.js";let c=fs.readFileSync(f,"utf8");c=c.replace("      daily_steps || null\n    );\n\n  } catch","      daily_steps || null\n    );\n    res.json({ id: result.lastInsertRowid, message: \"Check-in submitted\" });\n  } catch");c=c.replace("res.status(500).json({ error: \"Failed to submit check-in\" })","console.error(\"CHECKIN ERROR:\",err.message);res.status(500).json({ error: err.message })");fs.writeFileSync(f,c,"utf8");console.log("Done:",c.includes("Check-in submitted"));